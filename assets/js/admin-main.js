!(function($){
	"use strict";

	jQuery(document).ready(function($) {

	});

	jQuery(window).on('resize', function() {
    BeDemoSubmenuAuto();
	});

	jQuery(window).on('scroll', function() {

	});

})(jQuery);
